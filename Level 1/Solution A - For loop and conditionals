#Text to translate 

text = "g fmnc wms bgblr rpylqjyrc gr zw fylb. rfyrq ufyr amknsrcpq ypc dmp. bmgle gr gl zw fylb gq glcddgagclr ylb rfyr'q ufw rfgq rcvr gq qm jmle. sqgle qrpgle.kyicrpylq() gq pcamkkclbcb. lmu ynnjw ml rfc spj"



#The below function replaces each letter of plain text with a letter n places down the alphabet (default required for this solution is 2). Spacing and punctuation are preserved.

def lettershifter(text, size_shift=2):
    import string
    alphlist = list(string.ascii_lowercase)
    
    newtext = ""
    
    for t in text:
        if t in alphlist:
            if alphlist.index(t) + size_shift == 26:
                newtext += t.replace(t, 'a')
            elif alphlist.index(t) + size_shift == 27:
                newtext += t.replace(t, 'b')
            else:
                newtext += t.replace(t, alphlist[alphlist.index(t)+ size_shift])
        else:
            newtext += t
            
   return newtext.capitalize()
    
    
    
#Running lettershifter(text) should return the following
"""
    I hope you didnt translate it by hand. thats what computers are for. doing it in by hand is inefficient and that's why this text is so long. using string.maketrans() is recommended. now apply on the url
"""
    
    
    
